// Legacy file - 사용되지 않는 예전 회원가입 페이지
// 실제 회원가입 기능은 다른 곳에서 구현됨
import 'package:flutter/material.dart';

class LegacyRegistrationPage extends StatelessWidget {
  const LegacyRegistrationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Legacy Registration Page - Not in use'),
      ),
    );
  }
}
